import styles from './corona.module.scss'

import { useAppSelector } from 'hooks'

const Corona = () => {
  // const todoList = useAppSelector(getCorona)

  return (
    <div className={styles.todoList}>
      <div className={styles.centering}>dddd</div>
    </div>
  )
}

export default Corona
